# Specparam-FOOOF-Matlab-Wrapper
A MATLAB wrapper for the specparam (also known as FOOOF) toolbox. This repository includes additional functions for processing the LFP data into PSD to input into the specparam algorithm.
